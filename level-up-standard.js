/* Level Up UX Standard v1 — additive adapter for packaged Interview Arena */
(()=>{const q=(...s)=>s.map(x=>document.querySelector(x)).find(Boolean);const click=e=>e&&!e.disabled&&e.click();function boot(){document.body.classList.add('level-up-standard');const stage=q('.stage','.scene-frame','.game-stage','.scene-stage');if(!stage)return setTimeout(boot,250);if(document.querySelector('.lu-scene-rail'))return;const host=stage.parentElement;if(!host)return;const wrap=document.createElement('div');wrap.className='lu-standard-wrap';host.insertBefore(wrap,stage);const stageHost=document.createElement('div');stageHost.className='lu-stage-host';wrap.append(stageHost);stageHost.append(stage);const rail=document.createElement('aside');rail.className='lu-scene-rail';rail.setAttribute('aria-label','Scene controls');wrap.append(rail);const video=q('video');if(video&&getComputedStyle(stage).position==='static')stage.style.position='relative';if(video){const back=document.createElement('div');back.className='lu-caption-backdrop';stage.insertBefore(back,stage.firstChild)}const add=(label,cls,fn)=>{const b=document.createElement('button');b.type='button';b.textContent=label;if(cls)b.className=cls;b.onclick=fn;rail.append(b);return b};const audio=add('🔊 Audio','',()=>{if(!video)return;video.muted=!video.muted;audio.textContent=video.muted?'🔇 Audio':'🔊 Audio'});add('↺ Replay','',()=>{if(video){video.currentTime=0;video.play().catch(()=>{})}else click(q('[aria-label*="Replay"]','.replay-narration'))});add('⏭ Skip','',()=>click(q('.skip-narration','[aria-label*="Skip"]')));const play=add('▶ Play / Pause','',()=>{if(video){video.paused?video.play().catch(()=>{}):video.pause()}else click(q('.narration-btn','.resume-narration','[aria-label*="narration"]'))});const cont=add('Continue →','lu-continue',()=>click(q('.next-world','.scene-continue','#nextBtn','button.primary')));const sync=()=>{const original=q('.next-world','.scene-continue','#nextBtn','button.primary');cont.disabled=!!original?.disabled;cont.hidden=!original;};new MutationObserver(sync).observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['disabled','hidden','class']});sync()}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot):boot()})();
